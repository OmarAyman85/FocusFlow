{
  "name": "focusflow",
  "version": "1.0.0",
  "private": true,
  "description": "Orchestration scripts for running the FocusFlow backend and frontend together",
  "scripts": {
    "dev": "concurrently -n backend,frontend \"npm --prefix backend start\" \"npm --prefix frontend start\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
